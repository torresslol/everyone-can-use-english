## 2024-05-24 - Pure Markdown Repository Constraints
**Learning:** This repository consists entirely of Markdown files for a static content project. Application-level codebase optimizations (e.g., React, DB, API) do not apply here. Any HTTP to HTTPS upgrades or URL un-shortenings are strict CI requirements rather than optional performance micro-optimizations.
**Action:** When tasked with performance optimization in this repository, recognize that no suitable code-level optimizations exist and stop without creating a PR.
